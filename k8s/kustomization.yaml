apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: plumb

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml
  - pdb.yaml

# Single place to point every manifest at your build.
images:
  - name: ghcr.io/singhgauravlinux-coder/plumb
    newName: ghcr.io/singhgauravlinux-coder/plumb
    newTag: sha-eaff408
